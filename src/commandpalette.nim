import std/os
import seaqt/[qwidget, qdialog, qlineedit, qformlayout, qvboxlayout, qhboxlayout,
              qdialogbuttonbox, qpushbutton, qfiledialog]

proc showCommandPalette*(parent: QWidget): string =
  discard

when isMainModule:
  discard
